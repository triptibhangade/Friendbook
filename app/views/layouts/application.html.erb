<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Friendbook" %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- For third-generation iPad with high-resolution Retina display: -->
    <!-- Size should be 144 x 144 pixels -->
    

    <%= javascript_include_tag "application" %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
    <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.7.2/html5shiv.min.js" type="text/javascript"></script>
    <![endif]-->
  </head>
  <body>

    <div class="navbar navbar-default navbar-static-top">
      <div class="container">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <% if current_user.present?  %> 
          <a class="navbar-brand" href="/users"> Friendbook </a>
          <a class="navbar-brand" href="#">  </a>
          <div class="navbar-collapse collapse navbar-responsive-collapse">   
            <%= menu_group pull: :right do %>
              <%= menu_item "Sign Out", session_path, method: :delete %>
            <% end %>
            <%= menu_group pull: :right do %>
              <%= menu_item current_user.name, user_path(current_user.id) %>
            <% end %>
          </div>
        <% else %>         
          <a class="navbar-brand" href="/signin"> Friendbook </a>
          <a class="navbar-brand" href="#">  </a>
          <div class="navbar-collapse collapse navbar-responsive-collapse">   
            <%= menu_group pull: :right do %>
              <%= menu_item "Sign Up", signup_path, :class => "btn btn-xs btn-success" %>
            <% end %>
            <%= menu_group pull: :right do %>
              &nbsp
            <% end %>
            <%= menu_group pull: :right do %>
              <%= menu_item "Sign In", signin_path, :class => "btn btn-xs btn-info" %>
            <% end %>
          </div>
        <% end %>
        </div>
      </div>
    </div>

    <div class="container">
      <div class="row">
        <div class="col-lg-9">
          <%= bootstrap_flash %>
          <%= yield %>
        </div>
        <% if current_user.present?  %>
          <div class="col-lg-3">
            <div class="well sidebar-nav">
              <h3>Sidebar</h3>
              <ul class="nav nav-list">
                <!-- <li class="nav-header">Sidebar</li> -->
                <li><%= link_to "Friends", friends_path(current_user)  %></li>
                <li><%= link_to "Find Friends", users_path  %></li>
                <li><%= link_to "Friend Request", friendrequest_path(current_user) %></li>
                <li><%= link_to "Sent Request", sentrequest_path(current_user) %></li>
              </ul>
            </div><!--/.well -->
          </div><!--/span-->
        <% end %>
      </div><!--/row-->

      <footer>
        <p>&copy; Company 2019</p>
      </footer>

    </div> <!-- /container -->

  </body>
</html>
